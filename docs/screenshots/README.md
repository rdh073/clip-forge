# Screenshots

This directory holds the marketing assets referenced from the project README:

- `hero.gif` — animated GIF of `/clip-forge:start` running end-to-end
  (terminal recording, e.g. via [asciinema](https://asciinema.org) +
  [agg](https://github.com/asciinema/agg)).
- `clip-table.png` — screenshot of the clip-scout output table.
- `render-progress.png` — screenshot of streaming ffmpeg progress.

Drop the real captures here before tagging a release. The README links
expect these exact filenames.
